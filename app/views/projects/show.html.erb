<p id="notice"><%= notice %></p>

<h1><%= @project.name %></h1>

<p>
  <%= @project.description %>
</p>

<%= render :partial => "event", :collection => @project.events %>

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
